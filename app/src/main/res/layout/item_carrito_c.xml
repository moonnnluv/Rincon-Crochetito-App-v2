<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="10dp"
    app:cardUseCompatPadding="true">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="8dp">

        <!-- Imagen del producto -->
        <com.google.android.material.imageview.ShapeableImageView
            android:id="@+id/imagenPCar"
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:src="@drawable/item_img_producto"
            android:scaleType="centerCrop"
            android:adjustViewBounds="true"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Nombre -->
        <TextView
            android:id="@+id/nombrePCar"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="@string/nombrePCar"
            android:textColor="@color/azul_marino_oscuro"
            android:textSize="16sp"
            android:textStyle="bold"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="8dp"
            app:layout_constraintStart_toEndOf="@id/imagenPCar"
            app:layout_constraintTop_toTopOf="@id/imagenPCar"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Precio final -->
        <TextView
            android:id="@+id/precioFinalPCar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/precioFinalPCar"
            android:textSize="14sp"
            android:layout_marginStart="8dp"
            android:layout_marginTop="4dp"
            app:layout_constraintStart_toEndOf="@id/imagenPCar"
            app:layout_constraintTop_toBottomOf="@id/nombrePCar" />

        <!-- Precio original -->
        <TextView
            android:id="@+id/precioOriginalPCar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/precioOriginalPCar"
            android:textSize="13sp"
            android:layout_marginStart="8dp"
            android:layout_marginTop="4dp"
            app:layout_constraintStart_toEndOf="@id/precioFinalPCar"
            app:layout_constraintTop_toBottomOf="@id/nombrePCar" />

        <!-- Botón disminuir -->
        <ImageButton
            android:id="@+id/btnDisminuir"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@null"
            android:src="@drawable/ico_disminuir"
            android:layout_marginTop="8dp"
            app:layout_constraintStart_toEndOf="@id/imagenPCar"
            app:layout_constraintTop_toBottomOf="@id/precioFinalPCar" />

        <!-- Cantidad -->
        <TextView
            android:id="@+id/cantidadPCar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/cantidadPCar"
            android:textSize="16sp"
            android:layout_marginStart="8dp"
            app:layout_constraintStart_toEndOf="@id/btnDisminuir"
            app:layout_constraintBottom_toBottomOf="@id/btnDisminuir" />

        <!-- Botón aumentar -->
        <ImageButton
            android:id="@+id/btnAumentar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@null"
            android:src="@drawable/ico_incrementar"
            android:layout_marginStart="8dp"
            app:layout_constraintStart_toEndOf="@id/cantidadPCar"
            app:layout_constraintBottom_toBottomOf="@id/btnDisminuir" />

        <!-- Botón eliminar (X arriba a la derecha) -->
        <ImageButton
            android:id="@+id/btnEliminar"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:background="@android:color/transparent"
            android:src="@drawable/ic_close_cart"
            app:tint="@color/morado_medio"
            android:contentDescription="Eliminar del carrito"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />



    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>
